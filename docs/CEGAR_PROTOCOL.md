# CEGAR Protocol

Freeze prime pool and exact quotient semantics. Master chooses phases. Adversary returns an exact uncovered quotient state. Every counterexample becomes a cut. `ADVERSARY_UNSAT` certifies a cover only over the frozen exact domain; `MASTER_UNSAT` kills only the frozen pool and returns an obstruction core.
